# Contract service

FastAPI service for pricing calculations, requisites parsing, safe preview, and DOCX/PDF rendering.

The checked-in tariff, executor, customer, address, banking, and map values are synthetic and deliberately invalid for real transactions. The document template is a technical demonstration, not legal advice or a production contract.

Run the calculation tests:

```bash
python -m pytest tests
```
